# Skip puppeteer's bundled Chromium download on CI/Vercel.
# We use @sparticuz/chromium in production and puppeteer's own Chromium only
# in local dev — but Vercel installs devDependencies during build, so without
# this flag it downloads ~150 MB of Chromium it never uses.
puppeteer_skip_download=true
