'use client';

import { StepShell } from './StepShell';

/** Step 7 — Completion. Filled in fully in Task 17 (with redirect to /showcase). */
export function Step7() {
  return (
    <StepShell
      eyebrow="ALL SET"
      title="You're cleared for launch."
      lede="Your project is ready to join the showcase. The redirect-to-/showcase wiring lands in Task 17."
    />
  );
}
