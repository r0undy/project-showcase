'use client';

import { StepShell } from './StepShell';

/** Step 4 — UI/UX Setup. Accordion content lands in Task 20.2. */
export function Step4() {
  return (
    <StepShell
      eyebrow="STEP 4"
      title="UI / UX setup."
      lede="Walk through the design fundamentals: tokens, components, and the layout system. Accordion sections + Mark-as-Done arrive in Task 20.2."
    />
  );
}
