'use client';

import { StepShell } from './StepShell';

/** Step 3 — Information. Filled in fully in Task 17. */
export function Step3() {
  return (
    <StepShell
      eyebrow="STEP 3"
      title="Here's what you'll need."
      lede="A short rundown of the tools and accounts you'll touch over the next four steps. Final copy + visuals land in Task 17."
    />
  );
}
