'use client';

import { StepShell } from './StepShell';

/** Step 6 — Deployment Setup. Accordion content lands in Task 20.4. */
export function Step6() {
  return (
    <StepShell
      eyebrow="STEP 6"
      title="Deployment setup."
      lede="Connect AWS, configure the deploy pipeline, and ship a preview. Accordion sections arrive in Task 20.4."
    />
  );
}
