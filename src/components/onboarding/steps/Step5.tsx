'use client';

import { StepShell } from './StepShell';

/** Step 5 — Frontend Setup. Accordion content lands in Task 20.3. */
export function Step5() {
  return (
    <StepShell
      eyebrow="STEP 5"
      title="Frontend setup."
      lede="Set up your Next.js project, wire Tailwind, and pull in the components you'll need. Accordion sections arrive in Task 20.3."
    />
  );
}
